export class Animal {
  constructor(public species: string, public name: string, public age: number, public diet: string, public zooLocation: string, public caretakersNeeded: number, public sex: string, public like: string, public dislike: string, public dateLogged: string) { }
}
